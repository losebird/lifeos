---
date: 2025-08-26
tags:
  - daily
  - example
dq_goals: 7
dq_progress: 8
dq_meaning: 6
dq_happy: 8
dq_relationships: 5
dq_engaged: 9
habit_journal: true
habit_exercise: true
habit_reading: false
---
« [[Compass Dashboard]] » (seed note from "a year ago"; the normal navigation line is omitted because its week and quarter notes do not exist in the template)

# Tuesday, August 26, 2025

> [!theme]- Life theme
> ![[Life Theme#Theme]]

## 今天
- [x] Example task that was done this day ✅ 2025-08-26

## 日记
- 21:10 Example entry from "a year ago". Open today's note ([[2026-08-26]]) and this shows up under **On this day**. That is the whole point of journaling in the vault: bumping into your past self when you were not expecting to.

## 胜利
- Shipped the first draft of the newsletter before lunch.

## 感恩
- A slow 3-mile run while getting over a cold. Rated it a 9 for effort, not because the run was good.

## 每日问答
Answered via the prompt (see properties above).

## 往年今日
```dataviewjs
const me = dv.current().file.name;
const cfg = dv.page("Meta/Compass Config") || {};
const folder = cfg.daily_folder || "01 Journal/Daily";
if (/^\d{4}-\d{2}-\d{2}$/.test(me)) {
  const mmdd = me.slice(4);
  const yr = parseInt(me.slice(0, 4));
  const hits = dv.pages(`"${folder}"`).where(p => p.file.name !== me && p.file.name.endsWith(mmdd)).sort(p => p.file.name, "desc");
  if (hits.length === 0) dv.paragraph("*Nothing from previous years yet. Drop a marker future you will find useful.*");
  for (const p of hits) {
    const n = yr - parseInt(p.file.name.slice(0, 4));
    dv.header(4, `${n} year${n === 1 ? "" : "s"} ago: ${p.file.link}`);
    dv.paragraph(`![[${p.file.name}#日记]]`);
  }
}
```
